{
  "name": "thockr-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "docker compose up -d && concurrently --names frontend,backend,db --prefix-colors blue,green,yellow \"pnpm --filter @thockr/frontend dev\" \"pnpm --filter @thockr/backend dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vitest": "^4.1.6"
  },
  "packageManager": "pnpm@11.1.1"
}
